/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    reactStrictMode: true,
    // basePath: "/rbits",
    // assetPrefix: '/rbits/',
}

module.exports = nextConfig
